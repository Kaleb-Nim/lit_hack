import type { ReactNode } from "react";
import "./review.css";

/** Loads the review-route styles once for the clause and full-document pages. */
export default function ReviewLayout({ children }: { children: ReactNode }) {
  return children;
}
